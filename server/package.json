{
  "name": "geovistorias-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 3001",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute geovistorias --file=schema.sql",
    "db:init:remote": "wrangler d1 execute geovistorias --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
