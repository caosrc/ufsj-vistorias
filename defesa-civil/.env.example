# URL do backend deployado (Cloudflare Worker)
# Netlify: configurar como variável de ambiente VITE_API_URL no painel
# Desenvolvimento local: deixar vazio — Vite proxy redireciona /api para localhost:5000
VITE_API_URL=https://geovistorias.SEU-SUBDOMINIO.workers.dev
